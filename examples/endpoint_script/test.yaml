script:
  name: "endpoint script"
  description: ""
  type: "python"
  path: "./script.py"

exec:
  exec-env: "default-pyenv" 
  # script: ""
  # context: ""
  # env-file: ""
  env:
    - "TEST=1"
    - "TEST=true"

  args:
    - "arg1"
    - "arg2"

endpoint:
  name: "script"
  namespace: "test" # namespace must be created before hand
  private: false # private or public endpoint
  method: "get" # get or post endpoint method